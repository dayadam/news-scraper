{{#each articles}}
<div class="card m-5">
    <div class="card-header">
        <h4 class="card-title">{{this.title}}</h4>
    </div>
    <div class="card-body">
        <p class="card-text">{{this.link}}</p>
    </div>
</div>
{{/each}}

{{!-- border border-dark --}}